<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Результаты - Солнечная активность</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            margin-top: 20px;
        }
        .solar-icon {
            font-size: 48px;
            margin-bottom: 20px;
        }
        .flare-card {
            border-left: 4px solid #FF9800;
            margin-bottom: 15px;
            padding: 15px;
            background: #f9f9f9;
            border-radius: 5px;
        }
        .no-flares {
            color: #666;
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="text-center mb-4">
            <div class="solar-icon">
                {% if solar_data.flares_count > 0 %}
                    🔥
                {% else %}
                    ☀️
                {% endif %}
            </div>
            <h1>Результаты поиска</h1>
            <p class="lead">Дата: <strong>{{ selected_date|date:"d.m.Y" }}</strong></p>
        </div>

        <div class="alert
            {% if solar_data.flares_count > 0 %}
                alert-warning
            {% else %}
                alert-info
            {% endif %}">
            <h4 class="alert-heading">
                {% if solar_data.has_data %}
                    {{ solar_data.message }}
                {% else %}
                    {{ solar_data.message }}
                {% endif %}
            </h4>
        </div>

        {% if solar_data.error %}
            <div class="alert alert-danger">
                <h5>Ошибка:</h5>
                <p>{{ solar_data.error }}</p>
            </div>
        {% endif %}

{% if solar_data.has_data and solar_data.flares and solar_data.flares_count > 0 %}
    <h3 class="mt-4 mb-3">Солнечные вспышки:</h3>
    {% for item in solar_data.flares %}
        <div class="flare-card">
            <p class="mb-1"><strong>Начало:</strong> {{ item.begin_time }}</p>
            <p class="mb-1"><strong>Окончание:</strong> {{ item.end_time }}</p>
        </div>
    {% endfor %}
{% endif %}
{% if solar_data.flares_second and solar_data.flares_count_second > 0 %}
    <h3 class="mt-4 mb-3">Солнечные вспышки:</h3>
    {% for key, value in solar_data.flares_second.items %}
        <div class="flare-card">
            <p class="mb-1"><strong>Начало:</strong> {{ value.MinorProb }}</p>
            <p class="mb-1"><strong>Окончание:</strong> {{ value.MajorProb }}</p>
        </div>
    {% endfor %}
{% endif %}
        <div class="mt-4">
            <a href="{% url 'index' %}" class="btn btn-outline-primary">
                ← Назад к поиску
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>